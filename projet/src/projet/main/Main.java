package projet.main;

public class Main {
	public static void main(String args[]) {
	  int[][] tableau;
	  tableau = new int[2][2];
	  for(int i = 0; i < tableau.length; i++) {
	    for(int j = 0; j < tableau.length; j++) {
	      tableau[i][j] = 1;
	    }
	  }
		var listOfPatch = List.of(
		    new Patch(1,3,4,));
	}
}
